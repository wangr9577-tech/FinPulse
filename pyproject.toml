[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "finpulse-backend"
version = "0.1.0"
description = "FinPulse Intelligent Financial Research & News Backend"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["app*", "scripts*"]
