# =================================================================
# 智能投研信息引擎 (FinPulse Backend) - 依赖声明
# =================================================================
# 本项目的 Python 依赖已统一迁移至 pyproject.toml 的 [project] dependencies
# 中统一管理（符合 PEP 517/518 打包标准）。
#
# 安装依赖请使用（推荐，一次性安装全部依赖）：
#     pip install -e .
#
# 本文件保留仅为兼容旧的安装习惯；实际依赖解析一律以 pyproject.toml 为准。
# =================================================================
